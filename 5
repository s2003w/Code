clc;
clear;
close;
 
count = csvRead('C:\Users\Admin\OneDrive\Desktop\user defined functions\count.dat');
 
for i = 1:3
    scf();
    mu(i) = mean(count(:, i));
    sigma(i) = stdev(count(:, i));
    histplot(24, count(:, i));
    xtitle('(Roll no :E3263) Histogram of Traffic Intersection ' + string(i));
end
 
MeanTotal = mean(mean(count));
 
disp(mu, "Mean for individual Traffic Intersection is =");
disp(sigma, "Standard Deviation for individual Traffic Intersection is =");
disp(MeanTotal, "Overall Mean =");
 
